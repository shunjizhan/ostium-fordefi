# Alchemy API key for Arbitrum One RPC
ALCHEMY_API_KEY=your_alchemy_api_key_here

# ============================================
# Option 1: Local signer (direct private key)
# ============================================
# Private key for signing transactions (without 0x prefix)
PRIVATE_KEY=your_private_key_here

# ============================================
# Option 2: Fordefi MPC signer
# ============================================
# If FORDEFI_JWT_TOKEN is set, the SDK will use Fordefi instead of the local private key
# FORDEFI_JWT_TOKEN=your_jwt_token_here
# FORDEFI_PRIVATE_KEY_PATH=keys/pk.pem  # Path to P-256 private key PEM file (default: keys/pk.pem)
# FORDEFI_ADDRESS=0x_your_fordefi_wallet_address  # Optional, will auto-discover if not set
